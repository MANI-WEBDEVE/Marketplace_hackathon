/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['s3-alpha-sig.figma.com', 'shop-inam-ecommerce.vercel.app', 'cdn.sanity.io'],
    },
};

export default nextConfig;
